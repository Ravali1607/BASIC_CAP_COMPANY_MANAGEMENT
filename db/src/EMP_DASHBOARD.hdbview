VIEW "EMP_DASHBOARD"
AS SELECT E.EMP_ID, E.EMP_NAME, E.EMP_POSITION, D.DEPT_NAME, P.PROJ_NAME
FROM DB_EMPLOYEE E INNER JOIN DB_DEPARTMENT D 
ON E.DEPARTMENT_DEPT_ID = D.DEPT_ID INNER JOIN DB_PROJECT P
ON E.PROJECTS_PROJ_ID = P.PROJ_ID 